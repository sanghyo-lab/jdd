# DISC-20260921-agent-004 — 데모 누적 예산과 실제 검증 범위

| 항목 | 내용 |
| --- | --- |
| ID | DISC-20260921-agent-004 |
| 상태 | DISCUSSING |
| 제안 버전 | P2 |
| 작성자 / 정리 담당 | 한재홍 / agent |
| 영향받는 역할 | agent, voc, commerce/lead |
| 필수 합의자 | 한재홍, 김아름, 이상효 |
| 확인·답변 대기 | 배포 API 대상·scope·모델·예산·만료, Agent/lead 후속 정리 |
| 생성 시각 / 최종 갱신 | 2026-09-21T19:25:10+09:00 / 2026-09-21T21:16:00+09:00 |
| 다음 행동 / 담당 | P2 세 역할 영향 확인 완료. Agent/lead 배포 범위 정리·VOC worker/UI 구현·실제 모델 검증 준비 |

## P2 — 사용자 변경 지시 적용

2026-09-21T20:59:43+09:00 / 한재홍 / agent / P2

- 사용자 원문 요구는 “로컬 개발·데모 = Codex OAuth, 배포 = OpenAI API key”, “$50 API 크레딧은 배포에서만 사용”이다. 이 명시적 지시를 구현했으며 P1의 로컬 PC별 API 배분·활성화 계획은 더 이상 실행하지 않는다. 아래 P1 직접 답변과 실제 확인 기록은 이력으로 보존한다.
- `56cadd5`에 두 직접 HTTP Responses 어댑터, 강제 runtime/provider 조합, fallback 금지, 전용 CLI 최초 로그인·매 요청 file auth 조회, SSE/도구/보고서, 별도 OAuth usage, 예제·실행 명령을 공유했다. 전체 publish 종료 0: Python 51개·JUnit 146개(실패 0·조건부 건너뜀 9)·세 앱/실제 PostgreSQL smoke 통과. 실제 모델 호출은 0회다.
- local은 API key를 읽지 않고 deployed는 OAuth 파일을 읽지 않는다. 기본 Compose는 test/mock이다. OAuth는 각 개발자가 본인의 허용된 계정/워크스페이스로 프로젝트 전용 디렉터리에 새로 로그인한다. 기존 auth.json 복제·토큰 공유·공식 클라이언트 사칭·권한 우회는 하지 않는다.
- 기존 $30 유료 상한·영속 장부·미확정 비용 차단은 배포 API에서 유지한다. 배포 profile에 범위/배정/모델/만료를 정해야 한다. OAuth usage를 API 비용으로 환산하지 않으며 예산을 초기화하지 않는다. P1의 $5/$10/$15를 배포의 새 승인으로 재사용하지 않는다.
- 김아름 요청: 기존 v1 오류/재조사/근거 표시를 유지하고 provider 설정을 일반 UI에 추가하지 않는다. ngrok는 인증된 web만 공개하며 Agent/임의 모델 프록시를 직접 공개하지 않는다. 별도 ngrok 경로·허용 계정 YAML은 사용자가 추후 추가한다. Windows exporter 실패는 접수했으며 별도 수정/실제 Windows 인수로 추적한다. CLI 실행 분기에는 Windows 환경 변수/Gradle wrapper 처리를 추가하되 실제 Windows OAuth 로그인을 했다고 주장하지 않는다.
- 이상효 요청: 실제 모델 검증의 runtime/provider·모델·buildId와 OAuth/API 사용량을 분리해서 기록한다. 기존 P1의 수락을 P2 수락으로 간주하지 않고 직접 영향 확인을 남겨 달라. API 호출을 로컬에서 수행해 실제 모델 검증을 채우지 않는다.
- [실행 계약](../llm-runtime.md)과 [최신 비용 정책](../planning/demo-llm-policy.md)을 우선 적용한다. 이 정책 변경 구현은 상대 답변이나 실제 모델/팀 완료를 대신하지 않는다.

## P1 결정 질문 — 이전 계획 이력

데모 전체 $30을 넘기지 않도록 Agent PC $5, VOC PC $10, commerce·lead PC 합산 $15를 배분하고 아래 범위로만 실제 모델 검증을 준비할 것인가?

## 배경과 확인 근거

- [데모 정책](../planning/demo-llm-policy.md)과 [비용 계획](../planning/llm-efficiency-plan.md)은 공유 예산·리더의 새 검증·미확정 비용을 포함한다. $50 프로모션 표시는 $30 제한 확대나 개발용 사용 허가가 아니다.
- `1e179f3`의 Agent는 재시작·새 키·모델 변경으로 초기화할 수 없는 설치별 영속 장부, USD 상한·총 호출 수·조사별 호출 수·동시 수를 구현했다. 공용 원격 장부는 구현하지 않았다. 세 PC에 각각 $30을 설정하면 팀 전체 제한을 지킬 수 없다.
- `0ba2862`까지 모의 모델/실제 커머스 근거 검증과 전체 publish를 통과했다. 실제 모델 접근·품질·토큰/비용, VOC/web/ngrok는 미검증이다. 이 제안은 사용자의 승인이나 다른 담당자의 배분 수락을 대신하지 않는다.
- 키의 조직/프로젝트·프로모션 연결, 남은 잔액/기간, 다른 PC의 실제 장부는 직접 확인하지 않았다. API 키 값은 이 논의에서 요청하거나 기록하지 않는다.

## P1 제안과 대안

### 배분과 범위

| 실행 PC / 역할 | 데모 누적 USD 상한 | 실제 HTTP 시도 총상한 제안 | 대상 |
| --- | ---: | ---: | --- |
| 한재홍 / Agent | 5 | 88 | VOC-01~07·NORMAL·NEEDS_INPUT·IDEMPOTENCY·RECOVERY 11종 데모 검증 |
| 김아름 / VOC | 10 | 88 | 같은 11종의 실제 VOC/web 통합 검증 |
| 이상효 / commerce·lead | 15 | 176 | commerce 담당 11종과 별도 리더 11종 검증을 같은 장부에서 합산 |

- 호출 수는 목표가 아닌 최대 허용 시도다. 같은 키 재전송·조회/새로고침·근거 조회에는 0회, 새 조사·실패한 시도·보고서 수정을 포함한 모든 실제 HTTP 호출은 누적한다. 11개 case가 반드시 11개의 새 유료 조사를 뜻하지 않는다.
- 각 PC는 하나의 영속 DB/장부와 고유 scope를 사용한다. 검증 clone·DB를 새로 만들어 배분을 복제하지 않는다. commerce와 lead를 분리 배분하거나 장부를 초기화하지 않는다.
- 최초 후보는 `gpt-5.6-luna`, reasoning low, 조사당 최대 8회·180초, 출력 호출당 최대 4,096, 동시 유료 호출 1이다. 다른 모델 자동 전환은 없다. 모델 품질을 통과하기 전 최종 모델로 확정하지 않는다.
- 승인 뒤의 명시적 프로필은 최대 24시간만 유효하다. 기간 연장은 같은 scope/장부/배분을 유지하며 실제 데모 범위를 다시 확인한다. 예제의 만료값을 미리 활성 시각으로 바꾸지 않는다.
- 2026-09-21 버전 가격과 전체 모델 입력 상한으로 예약하는 Luna 호출당 최대치는 $0.5323728이다. 88회를 모두 최대치로 소비하면 $46.8488064이므로 **호출 수만으로 예산을 보장하지 않는다**. 매 호출 전 확정+미확정+예약+새 호출 최대치를 검사해 해당 PC 배분을 넘을 호출을 차단한다. $5 안에서 모든 case를 완료할 수 있다는 보장은 없다.
- 실측 비용·지연·정답 품질을 기록해 성공 조사당 총비용을 평가한다. 미확정 usage는 0으로 채우지 않는다. 한도 때문에 완료하지 못한 case는 실패/미검증으로 남긴다. 중단한 호출의 예약액을 임의 환급하지 않는다.

### 실행 순서와 책임

1. 세 담당자가 배분·동일 장부 사용을 직접 답변한다. 필요한 다른 배분은 총합 $30 이하인 새 제안으로 합의한다.
2. 한재홍은 사용자가 승인한 서비스 데모 범위와 로컬 비밀 파일 경로를 확인한다. 키 값은 브라우저/문서/로그에 넣지 않는다. 남은 API 접근 조건은 실제 확인 전 가정하지 않는다.
3. 각 PC의 기존 장부·QUEUED/RUNNING·프로필을 확인한다. 첫 VOC-07은 수동으로 실행하고 응답 모델·usage·보고서 근거를 검토한다. 미확정 과금이나 실패가 있으면 원인을 기록하고 다음 유료 호출을 중단한다.
4. 준비된 나머지 데모 case를 순차 실행한다. 반복 커머스 재현은 별도로 수행하며 일반 check/publish/CI·원격 상태 확인에서 유료 실행을 하지 않는다. UI가 준비되기 전의 로컬 API 검증과 ngrok PC/모바일 검증을 구분한다.
5. 실제 완료 기록은 최신 필수 검증과 내용 해시에 맞춰 담당자가 작성한다. 선택 메타데이터의 DTO·전달 경로는 [DISC-20260921-voc-001](DISC-20260921-voc-001-runner-metadata.md) 결정대로 별도 합의하며 새 필수 완료 필드를 만들지 않는다.

- 대안: 중앙 비용 장부를 새로 구현하면 다중 PC 배분 변경은 유연하지만 공통 운영 기반이 더 필요하다. 현재 P1은 이미 구현한 설치별 장부와 명시적 배분으로 진행한다.
- 합의 전 독립 작업: 모의 모델·실제 PostgreSQL·상대 구현/정책 사본·web 검증, 실패 기록과 검증용 관측 내보내기 준비. 실제 키/모델은 활성화하지 않는다.

## P2 해소 기준

- [x] 세 작업자가 P2 runtime/provider 분리와 소비자 영향을 직접 확인했다.
- [ ] 배포 API의 대상·scope·모델·예산·만료와 실제 활성화 조건을 확인했다.
- [ ] 배포 API의 팀 누적 $30 제한·장부 보존과 OAuth 사용량의 별도 관측을 확인했다.
- [ ] 관련 기록/공유와 목록이 일치한다. 실제 모델 품질·전체 완료 판정은 별도로 유지한다.

## 답변 기록

### 한재홍 — agent

2026-09-21T19:25:10+09:00 / 한재홍 / agent / P1

- 의견: 제안 수락. 내 PC의 $5·최대 88회와 다른 PC를 포함한 합계 제한을 확인한 뒤 승인된 서비스 데모에만 사용한다. 개발·CI에는 사용하지 않는다.
- 구현 근거: `1e179f3`의 명시 활성화/누적 호출 제한, `0ba2862`의 일곱 근거 소비 검사. 실제 장부 PG 12개·전송 모의 14개·기본 실행 비활성 검증을 통과했다. 실제 모델 호출은 0회다.
- 남은 조건: 김아름·이상효의 직접 답변, 사용자 허용 범위와 비밀 설정. 완료/승인을 대신 작성하지 않는다.

### 한재홍 — agent 활성화 전 관측

2026-09-21T19:49:00+09:00 / 한재홍 / agent / P1

- 이상효의 직접 수락을 확인했다. 김아름의 P1 답변과 사용자에게 요청한 이 PC의 $5·88회·11종·최대 24시간 데모 범위 답변은 아직 없으며 승인으로 간주하지 않는다.
- 이 PC의 기존 Compose 기본 DB `jdd`를 REPEATABLE READ READ ONLY로 조회했다. demo_budget 0행·model_calls 0행·QUEUED/RUNNING 0행이다. `ledger-export/preactivation-state-20260921T1049Z.json`과 읽기 전용 장부 내보내기 `preactivation-20260921T1048Z.json`을 보존했다. 합성 전용 테스트 DB와 구분하며 이 관측은 다른 PC·제공자 계정 잔액을 뜻하지 않는다.
- 아직 실제 예산 배분·활성 프로필·API 키를 등록하지 않았다. 기존 별도 clone은 코드 검증용이고 새 유료 예산을 복제하지 않는다. 실제 모델 호출은 0회다. ngrok 3.39.11은 설치했지만 authtoken 설정 파일·공개 web·접근 허용 범위가 준비되지 않아 터널은 열지 않았다.

### 이상효 — commerce / lead

2026-09-21T19:32:00+09:00 / 이상효 / commerce·lead / P1

- 의견: 배분·검증 계획 수락. 이 PC의 commerce와 독립 lead 검증을 합산해 $15·최대 176회, 하나의 영속 장부로 제한하는 P1을 따른다. 조사당 최대 8회·180초·출력 4,096·동시 1회와 첫 VOC-07 수동 검토, 미확정 사용량 발생 시 중단을 수락한다. 이 답변은 사용자의 실제 키 사용 승인이나 다른 담당자의 동의를 대신하지 않는다.
- 실제 확인: 현재 기본 Compose의 Agent PostgreSQL 장부를 읽어 demo_budget 0행·model_calls 0행·QUEUED/RUNNING 0행을 확인했다. 로컬 원문 `runtime/submission/commerce-20260921-resumed/demo-ledger-before-activation.json`. 별도 테스트 DB의 모의 장부와 구분한다. 아직 실제 배분을 등록하거나 유료 클라이언트를 활성화하지 않았다.
- 인수 근거: V5를 포함한 실제 PostgreSQL 장부 12개와 HTTP 접수 10개, 모든 앱의 405/415/404 오류 DTO를 이 PC에서 검증했다. 실행 결과는 commerce/lead 상태에 연결했다. 키·프로모션의 실제 프로젝트/모델 접근과 잔액·기간은 확인하지 않았다.
- 남은 조건: 김아름의 배분 수락, 사용자의 구체적인 데모 사용 범위와 로컬 비밀 설정, 각 PC의 장부 확인. 일반 check/publish/상태 확인은 유료 호출 0회를 유지한다. 새 정책 사본·VOC/web/runner 인수와 실제 모델 품질은 별도로 남아 있다.

### 김아름 — voc

2026-09-21T20:39:02+09:00 / 김아름 / voc / P1

- 의견: 배분·검증 계획 수락. VOC PC의 누적 $10·최대 88회, 같은 영속 장부/scope, 조사당 최대 8회·180초·출력 4,096·동시 유료 호출 1과 첫 VOC-07 수동 검토를 따른다. 시도 상한이 예산 보장은 아니며 미확정 비용이 생기면 다음 유료 호출을 중단한다.
- 실제 확인: 이 PC의 기존 jdd-voc-publication 스택에서 READ ONLY 조회로 demo_budget 0행·model_calls 0행·QUEUED/RUNNING 0행을 확인했다. 원문 runtime/verification/voc-demo-ledger-before-activation.json, 관측 시각 2026-09-21T11:39:01.634316Z. 모델은 DISABLED이고 새 예산·프로필·키는 등록하지 않았다. 계정 전체 사용량이나 다른 PC를 직접 확인한 결과는 아니다.
- 이 PC의 코드 검증 clone을 별도 유료 배분으로 사용하지 않으며, 실제 데모 전에 사용할 하나의 기존 영속 장부와 scope를 확인한다. 이 답변은 사용자의 데모 활성화 허가·잔액·모델 접근 확인을 대신하지 않는다. 실제 모델·web/runner 인수는 아직 남아 있다.
- 추가 인수 요청: 제공된 export_model_calls.py는 이 Windows 환경에서 실패했다. 같은 환경 변수 허용 목록으로 docker compose version을 실행하면 compose 하위 명령을 찾지 못한다. DOCKER_CONFIG를 명시한 재시도도 실패했고 위 별도 읽기 전용 SQL만 성공했다. 공통 스크립트의 Compose 탐색/대체 실행과 Windows 환경 처리를 제공자에게 요청한다. 원문 내보내기 성공으로 기록하지 않는다.

### 이상효 — commerce / lead 정책 변경 확인

2026-09-21T21:02:00+09:00 / 이상효 / commerce·lead / P1 재검토

- `56cadd5`의 [새 실행 계약](../llm-runtime.md)과 비용 정책은 로컬 OAuth·배포 API·test/mock을 분리한다. P1의 세 로컬 PC API 배분은 현재 실행 방식에 맞지 않으므로 REOPENED로 기록한다. 과거 수락은 보존하되 새 배포 배분·OAuth 모델/범위의 수락으로 재사용하지 않는다.
- 내 PC 기본 앱은 `0c07ca9c7c53-2e36710d9930`의 test/mock이다. 기존 API 장부 읽기 전용 내보내기는 호출 0건/budget=null이며 OAuth/API 모델 호출은 없다. 프로젝트 전용 인증/명시 모델을 요청했고 기존 개발 세션의 인증을 복제하지 않는다.
- 한재홍에게 로컬 OAuth 검증 범위와 배포 API의 실제 대상/예산 후속안을 요청한다. 김아름에게 runner에서 API USD와 OAuth 관측을 구분하는 영향을 확인 요청한다. 팀 누적 $30 조건·기존 장부/미확정 값 보존·실제 모델 품질/화면 검증은 유지한다.
- `VOC-AGENT-EXPORT-001`은 리더가 `0c07ca9`에서 환경/Compose 탐색을 보완하고 이 PC 실제 PostgreSQL 내보내기를 통과했다. [리더 상태](../status/lead.md)의 LEAD-015처럼 Windows 실제 재검증/담당 확인은 아직 대기다.

### 이상효 — commerce / lead P2 직접 답변

2026-09-21T21:05:00+09:00 / 이상효 / commerce·lead / P2

- 의견: 수락·영향 확인. 동시 공유된 `de13f74`의 P2 원문과 앞선 내 재개 답변을 모두 보존했다. local/codex_oauth·deployed/openai_api·test/mock 분리, 로컬 API fallback 금지와 배포에만 기존 유료 상한/장부 적용을 따른다. runtime/provider·실제 모델·buildId·OAuth 관측/API 비용을 구분해 기록한다.
- 내 앞선 REOPENED는 P1 조건 변경 확인이었다. 정리 담당의 새 P2가 있어 현재 상태는 DISCUSSING으로 통합한다. 새 배포 범위/배분과 김아름의 직접 답변은 아직 미확인이다. P1의 $15/176회를 배포 또는 OAuth 사용 승인으로 재사용하지 않는다.
- `de13f74`의 OS 분기로 LEAD-016에서 지적한 무조건적인 getuid/getgid·POSIX 실행기 문제는 소스상 보완됐다. 네이티브 Windows 로그인/파일 마운트·모델 품질은 별도 인수가 필요하며 타 PC 성공을 대신 작성하지 않는다. `VOC-AGENT-EXPORT-001`의 내보내기 수정 `0c07ca9`도 소비자 재검증을 기다린다.

### 김아름 — voc P2 직접 답변·Windows exporter 인수

2026-09-21T21:16:00+09:00 / 김아름 / voc / P2

- 의견: 실행 구분과 VOC 영향 수락. local/codex_oauth·deployed/openai_api·test/mock을 따르고 P1의 이 PC $10/88회 배분을 OAuth 또는 배포 API 사용 승인으로 재사용하지 않는다. 새 배포의 대상·scope·모델·예산·만료는 아직 미확정이며 별도로 정리해야 한다.
- 소비자 구현: 기존 v1 입력 스냅샷·같은 키 재전송·새 입력의 새 키/이전 조사 ID·보고서 evidenceIds를 보존한다. 모델 설정·인증·예산 오류는 조사 실패로 표시하고 고객 정보 부족 NEEDS_INPUT, 전달 오류, 조회 오류와 구분한다. 자동 provider 전환이나 새 키를 발급한 자동 재조사를 구현하지 않는다. 저장 API는 4de1a98로 공유했고 실제 worker·화면은 다음 단위다.
- UI/공개 범위: 일반 화면에는 provider 설정·자격 증명을 넣지 않는다. ngrok는 접근 제어가 적용된 web과 고정된 VOC 중계 경로만 연결하며 Agent·임의 URL/모델 프록시를 직접 공개하지 않는다. 사용자 제공 ngrok 경로·허용 계정 YAML과 실제 브라우저 검증은 아직 준비되지 않았다.
- 관측: runner/검증에서 실제로 제공되는 runtime/provider·모델·buildId를 기록하고 OAuth 사용량과 API USD를 별개로 취급한다. 관측되지 않은 메타데이터·비용을 만들거나 기존 필수 완료 계약을 임의 확장하지 않는다. 이 PC의 4de1a986fe6b-32fc61debb7b는 test/mock이고 OAuth 로그인·실제 모델 호출은 수행하지 않았다.
- VOC-AGENT-EXPORT-001 실제 인수: 리더 수정 0c07ca9를 포함한 코드에서 `python agent-app/scripts/export_model_calls.py --compose-dir . --output ..\..\verification\voc-ledger-export-portable.json`이 추가 DOCKER_CONFIG 설정 없이 종료 0이다. 제한된 환경에서 선택한 명령은 `C:\Program Files\Docker\Docker\resources\bin\docker.EXE compose`다. 실제 PostgreSQL READ ONLY on / REPEATABLE READ, budget=null·calls=0이며 모델 호출은 없다. 원문 SHA-256은 8e76e2c14ee65f36a2b0af6bbe15f393e1a9aea1f1f90bae17020befa9d82b77이다. 이 자료는 해당 설치의 API 장부이며 OAuth 사용량·계정 잔액 확인이 아니다. 리더/Agent에게 소비자 인수를 전달하고 그들의 지적·완료 기록은 대신 수정하지 않는다.
- 세 역할의 P2 영향 답변은 모였지만 배포 범위·실제 활성화 조건과 소비자/모델 검증이 남아 DISCUSSING을 유지한다. 기존 P1 답변과 실패 기록은 보존한다.

## 결정·실행·검증

P1의 직접 수락은 이력으로 보존한다. P2의 runtime/provider 구분과 영향은 세 역할이 직접 확인했다. 배포의 대상·scope·모델·예산·만료가 남아 DISCUSSING이다. VOC는 Windows exporter 소비를 실제 검증했으며 이 결과를 OAuth 로그인·실제 모델 품질·화면 또는 팀 완료로 계산하지 않는다. 실제 활성화 조건과 소비자 구현/검증이 남아 RESOLVED는 아니다. 알려진 단가/예약과 실제 사용량을 구분한다.

## 해소 또는 재개 이력

2026-09-21T19:25:10+09:00 / 한재홍: OPEN 등록. 팀 합의와 사용자 데모 범위 확인을 기다리며 독립 검증을 계속한다.

2026-09-21T20:39:02+09:00 / 김아름: VOC 직접 수락과 실제 장부 조회를 기록해 AGREED로 갱신. 승인된 데모 조건·실제 배분 확인 전 미해소 유지.

2026-09-21T20:59:43+09:00 / 한재홍: 사용자 인증/비용 분리 지시를 P2로 반영. 이전 로컬 API 배분을 활성화하지 않으며 직접 영향 확인 전 미해소 유지.

2026-09-21T21:02:00+09:00 / 이상효: 56cadd5의 OAuth/배포 API 분리로 P1 적용 조건이 바뀌어 REOPENED. 후속안과 직접 답변 대기.

2026-09-21T21:16:00+09:00 / 김아름: P2 영향 직접 수락·Windows exporter 인수 완료. 배포 범위가 남아 DISCUSSING 유지, 본문과 목록의 대기 항목 갱신.
