# Server only. Set fresh local secrets; never use NEXT_PUBLIC_ for these values.
WEB_ORIGIN=http://127.0.0.1:3000
WEB_ACCESS_PASSWORD=
WEB_SESSION_SECRET=
VOC_API_BASE_URL=http://127.0.0.1:8082
COMMERCE_API_BASE_URL=http://127.0.0.1:8080
# Optional only when receiving services enforce the same token.
BACKEND_SERVICE_TOKEN=
